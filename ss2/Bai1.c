#include <stdio.h>

int main() {
    // Kiểu số nguyên (integer)
    int soNguyen = 10;
    short soNguyenNgan = 123;
    long soNguyenDai = 123456L;

    // Kiểu số thực (floating point)
    float soThuc = 3.14f;
    double soThucDoi = 3.14159;

    // Kiểu ký tự (character)
    char kyTu = 'A';

    // Kiểu logic (boolean)
    int dung = 1;                // Thường dùng int để biểu diễn true (1)
    int sai = 0;                 // Biểu diễn false (0)

    // Kiểu không dấu (unsigned)
    unsigned int soKhongDau = 100u;  // Chỉ lưu số dương (0 đến 2^32-1)
    unsigned char byte = 255;        // Số không dấu 8-bit (0-255)

    // Hằng số (constant)
    const int HANG_SO = 100;     // Giá trị không thể thay đổi sau khi khởi tạo




}